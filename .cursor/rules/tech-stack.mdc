---
description: installing new packages, using new technology, shell commands
globs: 
alwaysApply: false
---
This rule describes the types of technology you should ALWAYS use if applicable

# Allowlist
 * `uv` for package management
 * `pytest` for python unit tests
 * `docker-compose` for containerized applications
 * `venv` for Python virtual environment management
 * `fish` as the default shell
 * `bash` for shell scripts in the scripts directory

# Denylist
 * `pip` for package management